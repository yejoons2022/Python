import turtle as t

t.speed(0)       
t.pensize(2)    
t.hideturtle()    
t.onscreenclick(t.goto)      # 마우스 버튼을 누르면 t.goto 함수를 호출합니다.
                                      # 그 위치로 거북이가 움직이면서 선을 그립니다.
